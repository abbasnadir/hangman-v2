This folder documents the design decisions of the app, database and API endpoints.

Some of the files use [mermaid](https://mermaid.js.org) extension to render previews, so it's recommended to install meramid as a dev dependency to view the files locally.
```
pnpm install mermaid --save-dev
```